# Sensitivity analysis output

`tolerance_sensitivity.csv` is regenerated by the experiment script. It supports comparison of feasible tolerances without conflating the number of selected patterns with deviation quality.
